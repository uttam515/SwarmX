from setuptools import setup, find_packages

setup(
    name="swarmx",
    version="0.1.0",
    description="SwarmX Python SDK for Adaptive Distributed Workloads",
    packages=find_packages(),
    python_requires=">=3.8",
    install_requires=[],
)
